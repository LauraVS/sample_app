<% provide(:title, @user.name) %>
<div class="row">

  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for(@user, :size=>70) %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>

  <div class="span8">
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>

      <ol class="microposts">
        <%= render @micropostsList %>
        <%# Rails knows that @micropostsList is a list of microposts.
		    It automatically iterates through them and renders each of the microposts using the _micropost.html.erb%>        
      </ol>

      <%= will_paginate @micropostsList %>
    <% end %>
  </div>

</div>