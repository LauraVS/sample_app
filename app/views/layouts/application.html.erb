<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag "application", media: "all",
                                           "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
  </head>

<body>
    <%= render 'layouts/header' %>

    <div class="container">

      <%# Show message in page. The message is saved in the controller %>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "alert alert-#{key}") %>
        <%# 
          NOTA: La llamada al helper 'content_tag' es equivalente a hacer:
          <div class="alert alert-<%= key *>"><%= value *></div> 
          (ojo, reemplazando '*' por 'tanto por ciento' )
        %>
      <% end %>

      <%# Content page %>
      <%= yield %>

      <%# Foot %>
      <%= render 'layouts/footer' %>

      <%# Debug information only to the development environment %>
      <%= debug(params) if Rails.env.development? %>       

    </div>

  </body>
</html>
